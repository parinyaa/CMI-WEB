import { SubStringDescriptionPipe } from './sub-string-description.pipe';

describe('SubStringDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SubStringDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
