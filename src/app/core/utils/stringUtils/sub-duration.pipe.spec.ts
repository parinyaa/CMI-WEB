import { SubDurationPipe } from './sub-duration.pipe';

describe('SubDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new SubDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
