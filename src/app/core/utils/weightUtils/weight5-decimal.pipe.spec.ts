import { Weight5DecimalPipe } from './weight5-decimal.pipe';

describe('Weight5DecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new Weight5DecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
