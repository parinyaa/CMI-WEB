import { ThaiDatePipePipe } from './thai-date-pipe.pipe';

describe('ThaiDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ThaiDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
